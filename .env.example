# Example production .env file for wphub-pro
# Copy this file to .env and fill in your real values for production

# --- Supabase ---
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=
VITE_SUPABASE_SERVICE_ROLE_KEY=
VITE_SUPABASE_FUNCTIONS_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- App Metadata ---
VITE_APP_NAME=wphub
VITE_APP_DOMAIN=
VITE_WPHUB_APP_ID=
VITE_WPHUB_BACKEND_URL=

# --- Stripe (if used) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- Other (add as needed) ---
YOUR_PLATFORM_URL=

# --- Notes ---
# - All VITE_ variables are exposed to the frontend (Vite convention)
# - SUPABASE_ variables are used by edge functions (Deno)
# - Never commit your real .env file with secrets to version control
# - For production, fill in all required values from your Supabase and Stripe dashboards
